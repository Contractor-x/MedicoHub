## Admin
- When the admin makes someone  a pro it doesnt reflect immedately
- the announce feature is having issues, (shows a mongoDB problem) {Admin/Setting/Notification}
- Database is slow to load. (Caching)
- Mcamp create quiz is chopped
- in Admin setting the discount percentage goes more than 100% (that should not happen)
- profile Pic upload is buggy and chopped(basically not working at this point)
- The admin user icon admin should be dyanmic not static
- The start page welcome admin should be more beautiful
- in the user management where it would show the user DP picture
- the Admin DP should be visible at the upper right icon button
- in the Dashboard it should not show welcome admin but welcome then followed by the user name
- there is a Cohort issue: user said the admin and user cohort are not fused in real time. and make sure the cohort works well.

## User
- The student welcome screen dashboard should have the user DP
- The tthing should not always ask for university and course everytime
- if students have already been logged in, it should not take them to the dashboard but rather to their student account 
- if the student decides to go the dashboard their DP picture should show in the upper right icon
- make sure the user data and chars on all the metrics are all synced 

## Sjay
- needs to add IP to the white list
